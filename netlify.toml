# Only the functions directory is declared here. Build command and publish
# directory stay in the Netlify UI, where they already are - repeating them in
# this file would silently override those settings from the repo.
[functions]
  directory = "netlify/functions"
